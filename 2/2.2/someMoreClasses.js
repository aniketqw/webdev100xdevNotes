const now = new Date(); // Current date and time
console.log(now.toISOString()); // Outputs the date in ISO format

const map = new Map();
map.set('name', 'Alice');
map.set('age', 30);
console.log(map.get('name'));


const d = new Date();
// console.log(d.getFullYear()); // Outputs the current year
// console.log(d.getMonth()); // Outputs the current month (0-11)
// console.log(d.getDate()); // Outputs the current day of the month
// console.log(d.getHours()); // Outputs the current hour (0-23)
// console.log(d.getMinutes()); // Outputs the current minute
// console.log(d.getSeconds()); // Outputs the current second
// console.log(d.getMilliseconds()); // Outputs the current millisecond
// console.log(d.getTime()); // Outputs the number of milliseconds since January 1, 1970
// console.log(d.toString()); // Outputs the date as a string
// console.log(d.toLocaleString()); // Outputs the date in local format
// console.log(d.toUTCString()); // Outputs the date in UTC format
// console.log(d.toISOString()); // Outputs the date in ISO format
// console.log(d.toDateString()); // Outputs the date as a string
// console.log(d.toTimeString()); // Outputs the time as a string
